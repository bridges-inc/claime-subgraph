{
  "name": "claime-rinkeby",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "test": "graph test",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ claime-rinkeby",
    "create-local": "graph create --node http://localhost:8020/ claime-rinkeby",
    "remove-local": "graph remove --node http://localhost:8020/ claime-rinkeby",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 claime-rinkeby"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "0.23.1"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.24.1",
    "matchstick-as": "^0.2.0",
    "prettier": "^2.5.0",
    "source-map-support": "^0.5.21"
  },
  "resolutions": {
    "@graphprotocol/graph-ts": "0.23.1"
  }
}
